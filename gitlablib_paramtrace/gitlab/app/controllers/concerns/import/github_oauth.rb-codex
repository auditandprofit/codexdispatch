```json
{
  "url -> execute_request -> connection.run_request": {
    "user_controlled": "no",
    "evidence": "The request URL is taken from the static OAuth configuration (Gitlab::Auth::OAuth::Provider.config_for('github')) and not from any user input."
  },
  "opts[:params] -> execute_request -> req.params.update": {
    "user_controlled": "no",
    "evidence": "This flow does not supply any opts[:params] from user input when performing the token request; parameters come from library defaults."
  },
  "opts[:body] -> execute_request -> connection.run_request": {
    "user_controlled": "yes",
    "evidence": "The authorization code passed into get_token(code) originates from params[:code] provided by the user and is sent in the request body."
  }
}
```