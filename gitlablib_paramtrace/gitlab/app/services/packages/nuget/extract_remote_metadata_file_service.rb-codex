```json
{
  "context": {
    "user_controlled": "yes",
    "evidence": "The IO passed as `context` to `Zip::InputStream.open` is created from fragments fetched by `Gitlab::HTTP.get` using a user-supplied `remote_url`, so an attacker can fully control the zip data.",
    "trace": [
      "app/services/packages/nuget/extract_remote_metadata_file_service.rb:L54-L57",
      "app/services/packages/nuget/extract_remote_metadata_file_service.rb:L53-L55",
      "app/services/packages/nuget/extract_remote_metadata_file_service.rb:L46-L49",
      "app/services/packages/nuget/extract_remote_metadata_file_service.rb:L38-L43"
    ]
  },
  "decrypter": {
    "user_controlled": "no",
    "evidence": "No `decrypter` argument is passed to `Zip::InputStream.open`; it uses the default nil, so attackers cannot influence the decrypter object.",
    "trace": [
      "app/services/packages/nuget/extract_remote_metadata_file_service.rb:L54-L57"
    ]
  }
}
```