```json
{
  "ldap_filter_string": {
    "user_controlled": "yes",
    "evidence": "The `value` argument to `FilterParser.parse` comes directly from the model’s attribute (e.g. a form parameter) without any sanitization, so an attacker can supply arbitrary LDAP filter strings."
  }
}
```