```json
{
  "credential_source[:url]": {
    "user_controlled": "no",
    "evidence": "The workload identity integration code hard-codes the AWS metadata service URL in its credential_source logic rather than accepting it from user input."
  },
  "credential_source[:file]": {
    "user_controlled": "no",
    "evidence": "The integration implementation uses a fixed metadata file path for credential_source and does not expose it for external configuration."
  },
  "credential_source[:executable][:command]": {
    "user_controlled": "no",
    "evidence": "The executable command is defined by the integration’s internal federation logic, not derived from end-user data."
  },
  "credential_source[:executable][:output_file]": {
    "user_controlled": "no",
    "evidence": "The output_file path is set in code for handling subprocess output and isn’t driven by any user-supplied configuration."
  },
  "credential_source[:region_url]": {
    "user_controlled": "no",
    "evidence": "The AWS region metadata URL is constructed from a validated region constant in integration code, not from arbitrary input."
  },
  "credential_source[:regional_cred_verification_url]": {
    "user_controlled": "no",
    "evidence": "The regional credential verification endpoint is generated by the AWS federation logic in code and cannot be overridden by users."
  },
  "credential_source[:imdsv2_session_token_url]": {
    "user_controlled": "no",
    "evidence": "The IMDSv2 session token URL is hard-coded in the federation implementation and not exposed as a user-configurable value."
  },
  "token_url": {
    "user_controlled": "no",
    "evidence": "The STS token exchange URL is determined by the external-account credential flow code and is not taken from user input."
  },
  "service_account_impersonation_url": {
    "user_controlled": "uncertain",
    "evidence": "The impersonation URL is built using the integration’s service-account identifier, which users configure, but the code enforces a fixed hostname."
  },
  "subject_token_type": {
    "user_controlled": "no",
    "evidence": "Subject token types are selected by fixed code paths based on integration type and are not directly supplied by users."
  }
}
```