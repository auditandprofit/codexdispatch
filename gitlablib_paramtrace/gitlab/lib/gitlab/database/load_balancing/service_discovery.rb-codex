```json
{
  "config[:config_file]": {
    "user_controlled": "no",
    "evidence": "ServiceDiscovery calls Net::DNS::Resolver.new without specifying a config_file option, so it uses the default system resolv.conf which isn’t influenced by external users.",
    "trace": [
      "lib/gitlab/database/load_balancing/service_discovery.rb:L151-L155"
    ]
  },
  "ENV['RES_OPTIONS']": {
    "user_controlled": "no",
    "evidence": "Resolver.initialize reads ENV['RES_OPTIONS'] from the process environment at startup, which is controlled by the system, not by remote attackers.",
    "trace": [
      "lib/gitlab/database/load_balancing/service_discovery.rb:L151-L155"
    ]
  },
  "config[:nameservers]": {
    "user_controlled": "no",
    "evidence": "ServiceDiscovery passes nameservers derived from an internal DNS lookup of @nameserver configured by the application, not directly from user input.",
    "trace": [
      "lib/gitlab/database/load_balancing/service_discovery.rb:L151-L155"
    ]
  }
}
```